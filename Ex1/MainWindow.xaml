<Window ResizeMode="NoResize"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ex1"
        xmlns:Controls="clr-namespace:Ex1.controls"
        xmlns:GraphReg="clr-namespace:Ex1.controls.GraphReg" x:Class="Ex1.MainWindow"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="1200">
    <Grid Background="#FFA6A3A3">
        <Grid.RowDefinitions>
            <RowDefinition Height="312*"/>
            <RowDefinition Height="298*"/>
            <RowDefinition Height="161*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260*"/>
            <ColumnDefinition Width="220*"/>
            <ColumnDefinition Width="320*"/>
            <ColumnDefinition Width="320*"/>
        </Grid.ColumnDefinitions>

        <Controls:Joystick x:Name="Joystick"/>
        <Controls:videoControlView Width="800" Height="200" x:Name="VideoControl" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="4" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="197,288,197,-29" Grid.RowSpan="2"/>
        <Button x:Name="UploadCSVButton" Content="Choose CSV" HorizontalAlignment="Center" VerticalAlignment="Center" Width="75" Click="UploadCSVButton_Click" Grid.Row="1" Height="20" Margin="101,138,101,140"/>
        <GraphReg:GraphRegView x:Name="GraphReg" HorizontalAlignment="Center" Height="200" VerticalAlignment="Center" Width="300" Grid.Row="1" Grid.Column="2" Margin="20,48,21,50" Grid.ColumnSpan="2"/>
        <ListView x:Name="FeaturesListView" HorizontalAlignment="Center" Height="400" Width="200" VerticalAlignment="Center" SelectionChanged="FeaturesListView_SelectionChanged" ItemsSource="{Binding Definitions}" Grid.RowSpan="2" Grid.Column="1" Margin="17,105,18,105">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Features"/>
                </GridView>
            </ListView.View>
        </ListView>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Column="2" Height="216" Margin="20,48,21,48" Width="300">
            <TextBlock Text="{Binding DisplayFeature, UpdateSourceTrigger=PropertyChanged}"/>
            <Controls:GraphControl Height="200" Width="300" x:Name="DisplayFeatureGraphControl" Minimum_x="0" Maximum_x="{Binding Size}" Minimum_y="{Binding DisplayFeatureMinValue}" Maximum_y="{Binding DisplayFeatureMaxValue}" Points="{Binding ValuesOfDisplayFeature}" />
        </StackPanel>

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Column="3" Height="216" Margin="21,48,20,48" Width="300">
            <TextBlock Text="{Binding CorrolateFeature, UpdateSourceTrigger=PropertyChanged}"/>
            <Controls:GraphControl Height="200" Width="300" x:Name="CorrelatedFeatureGraphControl" Minimum_x="0" Maximum_x="{Binding Size}" Minimum_y="{Binding CorrolateFeatureMinValue}" Maximum_y="{Binding CorrolateFeatureMaxValue}" Points="{Binding ValuesOfCorrelateFeature}" />
        </StackPanel>
    </Grid>
</Window>
